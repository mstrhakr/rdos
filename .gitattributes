# Keep Linux build/runtime files on LF even when edited on Windows.
*.sh text eol=lf
Dockerfile text eol=lf
d2vm text eol=lf
build.sh text eol=lf
build-installer-iso.sh text eol=lf

# Thin-client runtime scripts and service/config files are consumed by Linux.
tcfiles/* text eol=lf
